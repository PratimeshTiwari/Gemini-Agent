{
  "name": "agent-cli-extension",
  "version": "1.13.0",
  "description": "Browser bridge for the Agent CLI",
  "scripts": {
    "build": "esbuild src/background/main.js --bundle --outfile=service-worker.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "jsdom": "^29.1.1",
    "marked": "^15.0.12"
  },
  "type": "module"
}
