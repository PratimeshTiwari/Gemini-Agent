{
  "name": "gemini-agent-extension",
  "version": "1.1.0",
  "description": "Browser bridge for the Gemini Agent CLI",
  "scripts": {
    "build": "esbuild src/background/main.js --bundle --outfile=service-worker.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
