{
  "name": "gemini-agent",
  "version": "1.0.0",
  "description": "Claude Code-style coding agent powered by Gemini Web",
  "private": true,
  "workspaces": [
    "server",
    "extension"
  ],
  "scripts": {
    "start": "npm run start --workspace=server",
    "dev": "npm run dev --workspace=server",
    "test": "npm run test --workspace=server"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jest": "^30.4.2"
  }
}
